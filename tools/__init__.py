"""
Tools package for Multi-Agent Procurement Management System.

Each tool is a pure function that:
- Accepts strictly typed inputs
- Returns a structured dict
- Handles all errors internally (never raises to agents)
- Is independently unit-testable
"""
